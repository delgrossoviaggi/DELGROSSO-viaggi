DELGROSSO GESTIONALE V204 — FULL SYNC LOCAL-AUTH SAFE

Fix principale:
- La V203 richiedeva una sessione Supabase Auth per eseguire sync_gestionale_integrity.
- Il Gestionale usa invece autenticazione locale (dg_session), quindi il comando mostrava "Sessione gestionale non autenticata" anche con login valido.
- V204 usa l'autenticazione locale per autorizzare l'azione UI e DG_SUPABASE_SYNC.pullAll() per rileggere tutte le tabelle dal Supabase gestionale.
- Il database mantiene i trigger server-side di coerenza già installati; non viene modificato lo schema in questa versione.
- Il comando non forza più un reload della pagina dopo la sincronizzazione.

Verifiche:
- 23 pagine HTML
- nuovo dg-v204-full-sync.js incluso
- nessun riferimento HTML a dg-v203-full-sync.js/css
